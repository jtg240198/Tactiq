[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[functions]
  node_bundler = "esbuild"

[build]
  ignore = "node_modules"

[build.environment]
  SECRETS_SCAN_OMIT_PATHS = "node_modules"
